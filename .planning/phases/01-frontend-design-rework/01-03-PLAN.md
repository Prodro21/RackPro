---
phase: 01-frontend-design-rework
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/Sidebar.tsx
  - src/components/ui/button.tsx
  - src/components/ui/select.tsx
  - src/components/ui/slider.tsx
  - src/components/ui/checkbox.tsx
  - src/components/ui/toggle.tsx
  - src/components/ui/card.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/tooltip.tsx
  - src/components/ui/command.tsx
  - src/components/ui/sonner.tsx
  - src/components/ui/input.tsx
  - src/components/ui/table.tsx
  - src/components/ui/label.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Sidebar shows compact controls with section labels matching mockup"
    - "Segment controls (3D Print/Sheet Metal, Monolithic/Modular, etc.) use pill style from mockup"
    - "Select dropdowns, sliders, checkboxes use mockup colors and sizing"
    - "All shadcn/ui components render correctly in both dark and light themes"
    - "All existing interactive behavior (select changes, slider moves, checkbox toggles) works unchanged"
  artifacts:
    - path: "src/components/Sidebar.tsx"
      provides: "Restyled sidebar with 272px width, compact controls, segment controls, section labels"
      min_lines: 100
    - path: "src/components/ui/button.tsx"
      provides: "Button variants using new token namespace"
    - path: "src/components/ui/select.tsx"
      provides: "Compact select with mockup colors"
  key_links:
    - from: "src/components/Sidebar.tsx"
      to: "src/store/useConfigStore.ts"
      via: "Zustand selectors for panel config state"
      pattern: "useConfigStore"
    - from: "src/components/ui/*.tsx"
      to: "src/index.css"
      via: "CSS variable references (both new tokens and backward-compat)"
      pattern: "var\\(--"
---

<objective>
Restyle the sidebar and all shadcn/ui primitives to match mockup design.

Purpose: The sidebar is the primary control surface. UI primitives (button, select, slider, etc.) are used throughout the app — restyling them here propagates to all pages.
Output: Restyled Sidebar with compact controls and segment controls, all shadcn/ui components updated to use new color tokens.
</objective>

<execution_context>
@/Volumes/TB4 990 Evo 2Tb/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/TB4 990 Evo 2Tb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-frontend-design-rework/01-CONTEXT.md
@.planning/phases/01-frontend-design-rework/01-RESEARCH.md
@.planning/phases/01-frontend-design-rework/01-01-SUMMARY.md
@mockups/dark-theme.html
@mockups/light-theme.html
@src/components/Sidebar.tsx
@src/components/ui/button.tsx
@src/components/ui/select.tsx
@src/components/ui/slider.tsx
@src/components/ui/checkbox.tsx
@src/components/ui/toggle.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Sidebar with compact controls, segment controls, and section labels</name>
  <files>src/components/Sidebar.tsx</files>
  <action>
Restyle `src/components/Sidebar.tsx` to match the mockup sidebar:

1. **Container:** `w-[272px] bg-bg-sidebar border-r border-border-subtle flex flex-col overflow-y-auto`

2. **Section labels:** Replace current section headers with mockup's 10px uppercase labels:
   ```tsx
   <div className="text-[10px] font-semibold tracking-[0.08em] uppercase text-text-tertiary mb-2 px-0.5">
     Panel Standard
   </div>
   ```

3. **Segment controls (replaces Toggle):** For binary choices (3D Print/Sheet Metal, Monolithic/Modular, Tray/Box), replace the existing Radix Toggle usage with segment pill controls matching the mockup:
   ```tsx
   <div className="flex gap-px bg-seg-bg p-[3px] rounded-md border border-seg-border">
     <button className={cn(
       "flex-1 px-2 py-[4px] text-[11px] font-medium rounded transition-all border border-transparent",
       isActive
         ? "bg-seg-active border-border-default text-text-primary shadow-sm"
         : "text-text-tertiary hover:text-text-secondary"
     )}>
       3D Print
     </button>
     <button className={cn(/* same pattern */)}>Sheet Metal</button>
   </div>
   ```

4. **Compact selects:** Update Select triggers to: `h-[30px] text-xs bg-bg-input border-border-default rounded-md px-[10px]`

5. **Compact sliders:** Ensure sliders have `bg-bg-input` track and `bg-accent` fill/thumb

6. **Checkboxes:** Use `border-border-default` border and `bg-accent` checked state

7. **Spacing:** Use tight spacing: `gap-2` between sections, `gap-1.5` within sections, `p-4` container padding

8. **Placed elements area:** When empty, show a dashed border placeholder:
   ```tsx
   <div className="border border-dashed border-border-default rounded-md p-4 flex items-center justify-center text-text-tertiary text-xs">
     No elements placed
   </div>
   ```

9. **Add Device / Add Connector buttons:** Compact style: `h-[30px] text-xs bg-bg-elevated border border-border-default text-text-secondary hover:text-text-primary hover:border-border-strong rounded-md`

10. **Cost block:** Card treatment with `bg-bg-card border border-border-default rounded-lg p-3`

**Important:** Preserve ALL existing state management, event handlers, and Zustand selectors. Only change className strings and markup structure. Keep all `useConfigStore` selectors, `onChange` handlers, `useEnclosure`, etc. exactly as they are.
  </action>
  <verify>Run `npm run dev`. Sidebar renders at 272px width with correct colors. Segment controls toggle between options. Selects open and change values. Sliders adjust values. Checkboxes toggle. All panel configuration changes still update the preview correctly. Both dark and light themes look correct.</verify>
  <done>Sidebar has compact controls, segment pill controls, section labels, and card-styled cost block. All interactive behavior preserved. Both themes render correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle all shadcn/ui primitives to use new theme tokens</name>
  <files>src/components/ui/button.tsx, src/components/ui/select.tsx, src/components/ui/slider.tsx, src/components/ui/checkbox.tsx, src/components/ui/toggle.tsx, src/components/ui/card.tsx, src/components/ui/dialog.tsx, src/components/ui/tooltip.tsx, src/components/ui/command.tsx, src/components/ui/sonner.tsx, src/components/ui/input.tsx, src/components/ui/table.tsx, src/components/ui/label.tsx</files>
  <action>
Update each shadcn/ui component to use the new mockup token namespace. For each file, replace old token class names with new ones. The backward-compat bridge handles most cases, but for visual accuracy update to the explicit new tokens.

**`button.tsx`:** Update variant classes:
- `default`: `bg-accent text-white hover:bg-accent/90`
- `destructive`: `bg-danger text-white hover:bg-danger/90`
- `outline`: `border border-border-default bg-transparent text-text-secondary hover:bg-bg-hover hover:text-text-primary`
- `secondary`: `bg-bg-elevated text-text-secondary hover:bg-bg-hover`
- `ghost`: `text-text-secondary hover:bg-bg-hover hover:text-text-primary`
- `link`: `text-accent-text underline-offset-4 hover:underline`

**`select.tsx`:** Update:
- Trigger: `bg-bg-input border-border-default text-text-primary`
- Content: `bg-bg-elevated border-border-default`
- Item hover: `focus:bg-bg-hover focus:text-text-primary`
- Label: `text-text-tertiary`

**`slider.tsx`:** Update:
- Track: `bg-bg-input`
- Range (filled): `bg-accent`
- Thumb: `bg-accent border-2 border-accent`

**`checkbox.tsx`:** Update:
- Border: `border-border-default`
- Checked: `bg-accent border-accent text-white`

**`toggle.tsx`:** Update (even though it may be less used after segment controls):
- Pressed: `bg-bg-elevated text-text-primary`
- Not pressed: `text-text-secondary hover:bg-bg-hover`

**`card.tsx`:** Update:
- Card: `bg-bg-card border-border-default text-text-primary`
- CardDescription: `text-text-secondary`

**`dialog.tsx`:** Update:
- Overlay: `bg-black/60`
- Content: `bg-bg-elevated border-border-default text-text-primary`
- Description: `text-text-secondary`

**`tooltip.tsx`:** Update:
- Content: `bg-bg-elevated border border-border-default text-text-primary`

**`command.tsx`:** Update:
- Container: `bg-bg-elevated border-border-default text-text-primary`
- Input: `bg-transparent text-text-primary placeholder:text-text-tertiary`
- Item: `text-text-secondary data-[selected=true]:bg-bg-hover data-[selected=true]:text-text-primary`
- Group heading: `text-text-tertiary`

**`sonner.tsx`:** Update the Toaster theme:
- Use `toastOptions` to set `className` or `style` for background `var(--bg-elevated)`, text `var(--text-primary)`, border `var(--border-default)`

**`input.tsx`:** Update:
- `bg-bg-input border-border-default text-text-primary placeholder:text-text-tertiary focus:border-border-focus`

**`table.tsx`:** Update:
- Header: `text-text-tertiary bg-bg-nav`
- Row: `border-b border-border-subtle hover:bg-bg-hover`
- Cell: `text-text-primary`

**`label.tsx`:** Update:
- `text-text-secondary text-xs font-medium`

**Critical:** Do NOT change component structure, props, or Radix primitive usage. ONLY change className strings. Every component must remain functionally identical.

**Remove `dark:` prefixed classes:** Since dark is now the default and `.light` handles overrides, remove any `dark:` Tailwind prefix classes from these components (they're redundant — the dark values are now the base values).
  </action>
  <verify>Run `npm run dev`. Open configurator — all controls (selects, sliders, checkboxes, buttons) render with correct colors. Open Command Palette (Cmd+K) — correct dark styling. Open Custom Device modal — dialog has correct styling. Test light theme toggle — all components switch to light colors. No visual artifacts or missing styles.</verify>
  <done>All 13 shadcn/ui components use new theme tokens, render correctly in both dark and light themes, and maintain all existing interactive behavior.</done>
</task>

</tasks>

<verification>
1. Sidebar renders at correct width with section labels, segment controls, compact selects/sliders
2. Segment controls toggle correctly (3D Print/Sheet Metal, Monolithic/Modular)
3. All shadcn/ui components render with new theme colors
4. Both dark and light themes look correct across all components
5. Command palette, dialogs, tooltips, toasts all use new colors
6. No `dark:` prefix classes remain in ui/ components
7. All interactive behavior preserved (no functional regressions)
</verification>

<success_criteria>
- Sidebar matches mockup with compact controls and segment pill toggles
- All UI primitives consistently use new theme tokens
- Both dark and light themes render correctly
- Zero functional regressions in form controls
</success_criteria>

<output>
After completion, create `.planning/phases/01-frontend-design-rework/01-03-SUMMARY.md`
</output>
