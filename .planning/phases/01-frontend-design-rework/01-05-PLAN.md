---
phase: 01-frontend-design-rework
plan: 05
type: execute
wave: 3
depends_on: ["01-01"]
files_modified:
  - src/components/CatalogBrowser.tsx
  - src/components/CatalogCard.tsx
  - src/components/CatalogCardGrid.tsx
  - src/components/CatalogSearchSidebar.tsx
  - src/components/wizard/WizardShell.tsx
  - src/components/wizard/StepNav.tsx
  - src/components/wizard/StepStandard.tsx
  - src/components/wizard/StepUHeight.tsx
  - src/components/wizard/StepDevices.tsx
  - src/components/wizard/StepConnectors.tsx
  - src/components/wizard/StepReview.tsx
  - src/components/wizard/StepExport.tsx
  - src/components/CustomDeviceModal.tsx
  - src/components/ExportTab.tsx
  - src/components/SpecsTab.tsx
  - src/components/PreflightReport.tsx
  - src/components/CommandPalette.tsx
  - src/components/Preview3D.tsx
  - src/components/UpdatePrompt.tsx
  - src/App.tsx
  - src/routes/catalog.tsx
  - src/routes/wizard.tsx
  - vite.config.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Catalog browser renders with correct theme colors in both dark and light modes"
    - "Wizard steps render with correct theme colors and navigation works"
    - "Export tab, Specs tab, and Preflight report use new theme tokens"
    - "Custom Device modal renders correctly in both themes"
    - "3D preview background matches the current theme"
    - "No hardcoded colors remain in any component (all use CSS variables or theme tokens)"
  artifacts:
    - path: "src/components/CatalogBrowser.tsx"
      provides: "Theme-aware catalog browser layout"
    - path: "src/components/CatalogCard.tsx"
      provides: "Theme-aware catalog cards with correct border and text colors"
    - path: "src/components/wizard/WizardShell.tsx"
      provides: "Theme-aware wizard shell layout"
    - path: "src/components/ExportTab.tsx"
      provides: "Theme-aware export options panel"
    - path: "src/components/SpecsTab.tsx"
      provides: "Theme-aware specs table"
  key_links:
    - from: "src/components/CatalogCard.tsx"
      to: "src/index.css"
      via: "New theme token classes"
      pattern: "bg-bg-card|text-text-primary|border-border"
    - from: "src/components/Preview3D.tsx"
      to: "src/hooks/useTheme.ts"
      via: "Reading current theme for Three.js canvas background"
      pattern: "useTheme|bg-main"
---

<objective>
Restyle all remaining pages and components to complete the visual design rework.

Purpose: After the theme foundation (Plan 01), layout (Plan 02), sidebar/UI (Plan 03), and SVG (Plan 04) are done, the remaining pages (Catalog, Wizard, Export, Specs, etc.) still need their colors updated to use the new theme tokens.
Output: All application pages and components consistently use the new theme system with zero hardcoded colors.
</objective>

<execution_context>
@/Volumes/TB4 990 Evo 2Tb/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/TB4 990 Evo 2Tb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-frontend-design-rework/01-CONTEXT.md
@.planning/phases/01-frontend-design-rework/01-RESEARCH.md
@.planning/phases/01-frontend-design-rework/01-01-SUMMARY.md
@mockups/dark-theme.html
@mockups/light-theme.html
@src/components/CatalogBrowser.tsx
@src/components/CatalogCard.tsx
@src/components/wizard/WizardShell.tsx
@src/components/ExportTab.tsx
@src/components/SpecsTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Catalog pages, Wizard pages, and route shells</name>
  <files>src/components/CatalogBrowser.tsx, src/components/CatalogCard.tsx, src/components/CatalogCardGrid.tsx, src/components/CatalogSearchSidebar.tsx, src/components/wizard/WizardShell.tsx, src/components/wizard/StepNav.tsx, src/components/wizard/StepStandard.tsx, src/components/wizard/StepUHeight.tsx, src/components/wizard/StepDevices.tsx, src/components/wizard/StepConnectors.tsx, src/components/wizard/StepReview.tsx, src/components/wizard/StepExport.tsx, src/routes/catalog.tsx, src/routes/wizard.tsx</files>
  <action>
**Catalog pages:**

`CatalogBrowser.tsx`:
- Update background: `bg-bg-main`
- Update text colors: headings → `text-text-primary`, descriptions → `text-text-secondary`
- Update any inline colors to use theme tokens

`CatalogCard.tsx`:
- Card background: `bg-bg-card`
- Card border: `border border-border-default hover:border-border-strong`
- Title: `text-text-primary`
- Description/meta: `text-text-secondary`
- Badges: `bg-accent-subtle text-accent-text` for data source badges
- Any price/cost text: `font-mono text-text-primary`

`CatalogCardGrid.tsx`:
- Grid container: `bg-bg-main`
- Empty state: `text-text-tertiary`

`CatalogSearchSidebar.tsx`:
- Sidebar background: `bg-bg-sidebar border-r border-border-subtle`
- Search input: `bg-bg-input border-border-default`
- Filter labels: `text-text-tertiary text-[10px] uppercase tracking-wider font-semibold`
- Filter checkboxes: Use restyled checkbox from Plan 03
- Active filters: `bg-accent-subtle text-accent-text`

`catalog.tsx` route: Update any wrapper div classes to use new tokens.

**Wizard pages:**

`WizardShell.tsx`:
- Outer container: `bg-bg-main`
- Card/panel areas: `bg-bg-card border border-border-default rounded-lg`
- Progress indicators: Active step uses `bg-accent text-white`, completed uses `bg-accent-subtle text-accent-text`, pending uses `bg-bg-elevated text-text-tertiary`

`StepNav.tsx`:
- Navigation buttons: Use new button variants from Plan 03
- Step indicators: Active = `text-text-primary font-semibold`, inactive = `text-text-tertiary`
- Connector lines between steps: `bg-border-default`, completed = `bg-accent`

`StepStandard.tsx`, `StepUHeight.tsx`:
- Radio/button groups: `bg-bg-card border border-border-default` inactive, `bg-accent-subtle border-accent` active
- Labels: `text-text-primary`, descriptions: `text-text-secondary`

`StepDevices.tsx`, `StepConnectors.tsx`:
- List items: `bg-bg-card border border-border-default hover:border-border-strong`
- Selected items: `border-accent bg-accent-subtle`
- Add buttons: Use accent button style

`StepReview.tsx`:
- Summary table: `bg-bg-card` with `border-border-default` rows
- Warning/info badges: `bg-warning/10 text-warning`, `bg-accent-subtle text-accent-text`

`StepExport.tsx`:
- Export option cards: `bg-bg-card border border-border-default hover:border-accent`
- Format descriptions: `text-text-secondary`

`wizard.tsx` route: Update any wrapper div classes.

**Approach for all files:** Replace every instance of old token classes (`bg-background`, `text-foreground`, `bg-card`, `text-card-foreground`, `bg-muted`, `text-muted-foreground`, `bg-secondary`, `bg-primary`, `text-primary-foreground`, `bg-destructive`, `border-border`, `bg-accent`, `text-accent-foreground`) with the new mockup token classes. Also replace any remaining hardcoded hex colors.

**Preserve all functional behavior.** Only change className strings and style attributes.
  </action>
  <verify>Run `npm run dev`. Navigate to Catalog page — cards display with correct colors in both themes. Navigate to Wizard — all 6 steps render correctly, navigation works, both themes look correct. Run `grep -rn "bg-background\|text-foreground\|bg-card\b\|text-card-foreground\|bg-muted\b\|text-muted-foreground" src/components/Catalog*.tsx src/components/wizard/*.tsx` — should return zero matches for old token usage in these files.</verify>
  <done>All catalog and wizard pages use new theme tokens. Both dark and light themes render correctly. No old token class names remain in these files.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle remaining components (ExportTab, SpecsTab, PreflightReport, modals, 3D preview, misc)</name>
  <files>src/components/ExportTab.tsx, src/components/SpecsTab.tsx, src/components/PreflightReport.tsx, src/components/CustomDeviceModal.tsx, src/components/CommandPalette.tsx, src/components/Preview3D.tsx, src/components/UpdatePrompt.tsx, src/App.tsx, vite.config.ts</files>
  <action>
**`ExportTab.tsx`:**
- Export format cards: `bg-bg-card border border-border-default hover:border-accent`
- Copy/download buttons: Use new button variants
- Code blocks: `bg-bg-input font-mono text-text-primary border border-border-subtle rounded-md`
- Success/error badges: `text-success` / `text-danger`
- Section headers: `text-text-primary font-semibold`
- Descriptions: `text-text-secondary`

**`SpecsTab.tsx`:**
- Table: Use new table component styling (bg-bg-nav header, border-border-subtle rows)
- Section headers: `text-text-primary`
- Values: `font-mono text-text-primary`
- Units/labels: `text-text-secondary`
- Warning indicators: `text-warning`
- Reference information: `text-text-tertiary text-xs`

**`PreflightReport.tsx`:**
- Pass badges: `bg-success-subtle text-success`
- Fail badges: `bg-danger/10 text-danger`
- Warning badges: `bg-warning/10 text-warning`
- Details: `text-text-secondary`
- Container: `bg-bg-card border border-border-default rounded-lg`

**`CustomDeviceModal.tsx`:**
- Dialog already restyled in Plan 03, but check for any remaining hardcoded colors in the modal content
- Input fields: `bg-bg-input border-border-default`
- Labels: `text-text-secondary`
- Form sections: appropriate spacing and colors

**`CommandPalette.tsx`:**
- Already partially handled by command.tsx restyling in Plan 03
- Check for any additional hardcoded colors in the component itself
- Update any remaining old token references

**`Preview3D.tsx`:**
- The Three.js canvas background needs to match the theme
- Read the current theme somehow. Options:
  1. Pass a `style` prop to the R3F `<Canvas>` with `background: 'var(--bg-main)'`
  2. If that doesn't work with WebGL, read the theme from `document.documentElement.classList.contains('light')` and set `scene.background` to the appropriate Three.js Color
- Also update any container div colors around the canvas

**`UpdatePrompt.tsx`:**
- Banner/notification styling: `bg-bg-elevated border border-border-default text-text-primary`
- Action buttons: Use new button variants

**`src/App.tsx`:**
- Error boundary fallback: Replace any inline style colors with theme tokens
- Background: `bg-bg-root`

**`vite.config.ts`:**
- Update PWA `theme_color` if defined (change to match dark theme background color, e.g., `#0c0d11`)

**Final sweep:** After all changes, do a project-wide search for any remaining old token class patterns:
```bash
grep -rn "bg-background\|text-foreground\|bg-card\b\|text-card-foreground\|bg-muted\b\|text-muted-foreground\|bg-secondary\b\|text-secondary-foreground\|bg-primary\b\|text-primary-foreground\|bg-accent\b\|text-accent-foreground" src/components/ src/routes/ src/App.tsx
```
Any remaining instances should be updated to new token names OR confirmed to be in backward-compat contexts (like shadcn/ui lib files that already bridge correctly).

Also search for hardcoded hex colors in non-SVG components:
```bash
grep -rn "'#[0-9a-fA-F]\{3,8\}'" src/components/ --include="*.tsx" --exclude-dir=__tests__
```
Replace any found with CSS variable references or Tailwind utility classes.
  </action>
  <verify>Run `npm run dev`. Check ExportTab — all export options render correctly in both themes. Check SpecsTab — tables display with correct colors. Open Custom Device modal — correct dialog styling. Open Command Palette (Cmd+K) — correct styling. View 3D preview — background matches theme. Toggle theme — all components update. Run `npx vite build` — no errors. Do final grep for old token classes across entire src/ — confirm minimal remaining usage (only in backward-compat bridge itself).</verify>
  <done>All remaining components use new theme tokens. 3D preview background adapts to theme. No old token class names remain in application components. Build succeeds. Both themes render correctly across all pages.</done>
</task>

</tasks>

<verification>
1. Every page in the app (Configurator, Catalog, Wizard) renders correctly in dark theme
2. Every page renders correctly in light theme
3. Theme toggle switches all pages simultaneously
4. No old shadcn token class names remain in application component files
5. No hardcoded hex colors in non-SVG component files
6. `npx vite build` succeeds with no errors or warnings
7. All existing functionality works (catalog search, wizard navigation, export, specs)
8. 3D preview background adapts to theme
</verification>

<success_criteria>
- Complete visual consistency across all pages and components
- Zero old token class names in application code (outside ui/ lib backward-compat)
- Zero hardcoded hex colors in components
- Build succeeds
- Both themes fully functional
</success_criteria>

<output>
After completion, create `.planning/phases/01-frontend-design-rework/01-05-SUMMARY.md`
</output>
