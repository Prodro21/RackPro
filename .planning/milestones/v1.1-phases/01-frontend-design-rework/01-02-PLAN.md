---
phase: 01-frontend-design-rework
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/routes/__root.tsx
  - src/routes/configurator.tsx
  - src/components/NavSidebar.tsx
  - src/components/Header.tsx
  - src/components/StatusBar.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Icon-nav shows on the left with orange accent bar on active item"
    - "Header is inside the main content area with brand logo, segmented pill tabs, and undo/redo + theme toggle"
    - "Tab bar uses segmented pill control style matching mockup"
    - "Theme toggle (sun/moon icon) works in the header toolbar"
    - "Status bar is 28px with monospace values and dot separators"
    - "Catalog and wizard routes still render correctly without sidebar"
  artifacts:
    - path: "src/components/NavSidebar.tsx"
      provides: "Restyled icon-nav with 52px width, orange accent bar on active, mockup colors"
      min_lines: 30
    - path: "src/components/Header.tsx"
      provides: "Brand logo, segmented pill tabs, undo/redo toolbar, theme toggle button"
      min_lines: 50
    - path: "src/components/StatusBar.tsx"
      provides: "28px status bar with dot separators and monospace values"
      min_lines: 20
    - path: "src/routes/__root.tsx"
      provides: "Root layout with icon-nav + outlet flex structure"
    - path: "src/routes/configurator.tsx"
      provides: "Configurator layout with sidebar + main area containing header, content, status bar"
  key_links:
    - from: "src/components/Header.tsx"
      to: "src/hooks/useTheme.ts"
      via: "useTheme() hook for toggle button"
      pattern: "useTheme"
    - from: "src/routes/__root.tsx"
      to: "src/components/NavSidebar.tsx"
      via: "NavSidebar rendered at root level"
      pattern: "NavSidebar"
    - from: "src/components/Header.tsx"
      to: "TanStack Router tabs"
      via: "Segmented pill buttons triggering route navigation"
      pattern: "useNavigate\\|Link"
---

<objective>
Restructure the app layout and restyle navigation, header, and status bar to match the mockup design.

Purpose: The layout structure (icon-nav | sidebar | main with header) is the visual skeleton that all other components sit within. Getting this right unblocks sidebar and content restyling.
Output: Updated root layout, restyled NavSidebar with orange accent bars, new Header with brand/logo/segmented tabs/theme toggle, restyled StatusBar.
</objective>

<execution_context>
@/Volumes/TB4 990 Evo 2Tb/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/TB4 990 Evo 2Tb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-frontend-design-rework/01-CONTEXT.md
@.planning/phases/01-frontend-design-rework/01-RESEARCH.md
@.planning/phases/01-frontend-design-rework/01-01-SUMMARY.md
@mockups/dark-theme.html
@mockups/light-theme.html
@src/routes/__root.tsx
@src/routes/configurator.tsx
@src/components/NavSidebar.tsx
@src/components/Header.tsx
@src/components/StatusBar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure root and configurator layouts, restyle NavSidebar with orange accent bar</name>
  <files>src/routes/__root.tsx, src/routes/configurator.tsx, src/components/NavSidebar.tsx</files>
  <action>
**Target layout structure (from mockup):**
```
[icon-nav (52px)] | [Outlet]
                    For configurator route:
                    [sidebar (272px)] | [main-area]
                                       ├── [header (48px)]
                                       ├── [content (flex-1)]
                                       └── [status-bar (28px)]
```

**`src/routes/__root.tsx`:**
- Keep the existing structure (NavSidebar at root, Outlet for content)
- Update the outer div: `bg-bg-root text-text-primary` (replace `bg-background text-foreground`)
- Ensure `NavSidebar` is the first child, `Outlet` fills remaining space

**`src/routes/configurator.tsx`:**
- Restructure: Sidebar on the left, then a flex-col container for Header + MainContent + StatusBar
- Import StatusBar here (move it from wherever it currently renders, likely MainContent)
- Structure:
  ```tsx
  <div className="flex flex-1 min-h-0 overflow-hidden">
    <Sidebar />
    <div className="flex flex-col flex-1 min-w-0 overflow-hidden">
      <Header />
      <MainContent />
      <StatusBar />
    </div>
  </div>
  ```

**`src/components/NavSidebar.tsx`:**
Restyle to match mockup's icon-nav:
- Width: `w-[52px]` (was `w-16` = 64px)
- Background: `bg-bg-nav` with right border `border-r border-border-subtle`
- Each nav item: `w-10 h-10` icon button centered in the 52px column
- Active state: Orange accent bar on the left edge using a pseudo-element (`before:content-[''] before:absolute before:left-[-6px] before:top-1/2 before:-translate-y-1/2 before:w-[3px] before:h-4 before:bg-accent before:rounded-r-sm`) plus `bg-bg-elevated text-text-primary`
- Inactive state: `text-text-tertiary hover:text-text-secondary hover:bg-bg-hover`
- Icon size: ~18px (use `size={18}` on lucide icons)
- Nav items should include small 8px labels under the icon (matching mockup: "Config", "Catalog", "Wizard", etc.)
- Bottom section: keep any settings/info icons at the bottom of the nav column
  </action>
  <verify>Run `npm run dev`. Navigate to configurator route — layout shows icon-nav | sidebar | (header + content + status bar). Active nav item has orange accent bar. Navigate to catalog/wizard routes — sidebar does not appear, full width content renders. No layout overflow or scroll issues.</verify>
  <done>Layout matches mockup structure: 52px icon-nav at root, sidebar within configurator, header inside main area. NavSidebar has orange accent bars on active items, correct colors and sizing.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle Header with brand logo, segmented pill tabs, and theme toggle; restyle StatusBar</name>
  <files>src/components/Header.tsx, src/components/StatusBar.tsx</files>
  <action>
**`src/components/Header.tsx`:**
Restyle to match mockup header (48px height, inside the main content area):

1. **Container:** `h-12 flex items-center px-4 bg-bg-main border-b border-border-subtle`

2. **Brand section (left):**
   - Logo: Orange gradient rounded square (~24x24px) with a simple rack/grid SVG icon inside (white lines forming a 2x2 or 3x2 grid pattern). Use inline SVG with `linearGradient` from `#ff6a1a` to `#ff4500`. Rounded corners `rx="5"`.
   - App title: "RackPro" in `text-sm font-semibold text-text-primary tracking-tight` immediately after logo

3. **Segmented tab bar (center):**
   Create a segmented pill control for the view tabs (Front, Side, Split, 3D, Specs, Export, Cost). Style:
   - Container: `flex gap-px bg-seg-bg p-[3px] rounded-lg border border-seg-border`
   - Active tab: `bg-seg-active border border-border-default shadow-sm text-text-primary`
   - Inactive tab: `text-text-tertiary hover:text-text-secondary hover:bg-bg-hover border border-transparent`
   - Button sizing: `px-3 py-[5px] text-[11px] font-medium uppercase tracking-wider rounded-md`
   - Use the existing tab switching logic — these buttons should trigger the same state changes as current tabs
   - Do NOT use Radix Toggle or Radix Tabs — use simple button group with active state

4. **Toolbar section (right):**
   - Undo/redo buttons: Keep existing functionality, restyle to match mockup (`text-text-tertiary hover:text-text-secondary` icons, small size)
   - Theme toggle button: Import `useTheme` hook. Render Sun icon (when dark) or Moon icon (when light). On click call `toggle()`. Same styling as undo/redo buttons.
   - Buttons in a `flex items-center gap-1` group

**`src/components/StatusBar.tsx`:**
Restyle to match mockup status bar:

1. **Container:** `h-7 flex items-center px-4 text-[11px] bg-bg-nav border-t border-border-subtle`

2. **Values:** Use `font-mono` class for all dimension/measurement values. Use `text-text-secondary` for labels, `text-text-primary` for values.

3. **Separators:** Add `·` (middle dot) characters between value groups with `text-text-tertiary mx-2` styling.

4. **Format:** Keep existing information (dimensions, element count, etc.) but restructure to: `Standard: 19" · 2U · 482.6 × 88.1 mm · 3 elements · Sheet Metal 16ga CRS`
  </action>
  <verify>Run `npm run dev`. Header shows: logo (orange gradient square) + "RackPro" on left, segmented pill tabs in center, undo/redo + theme toggle on right. Clicking theme toggle switches between dark and light. All tabs switch views correctly. Status bar is 28px with monospace values and dot separators.</verify>
  <done>Header has brand logo, segmented pill tab bar, and theme toggle. Status bar is 28px with dot-separated monospace values. Theme toggle persists preference and switches themes without flicker.</done>
</task>

</tasks>

<verification>
1. Full page layout: icon-nav (52px) | sidebar (272px) | main area with header + content + status bar
2. NavSidebar active item has orange accent bar on left edge
3. Header shows brand logo, segmented tabs, undo/redo, theme toggle
4. Segmented tabs switch views correctly
5. Theme toggle switches between dark and light themes
6. Status bar is 28px with monospace values and dot separators
7. Catalog and wizard routes render without sidebar (full width)
8. No layout overflow or z-index issues
</verification>

<success_criteria>
- Layout matches mockup 3-column structure
- Navigation, header, and status bar are visually consistent with mockup
- Theme toggle is functional and persisted
- All existing functionality (view switching, undo/redo, routing) still works
</success_criteria>

<output>
After completion, create `.planning/phases/01-frontend-design-rework/01-02-SUMMARY.md`
</output>
