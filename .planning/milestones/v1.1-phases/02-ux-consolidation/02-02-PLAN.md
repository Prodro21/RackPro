---
phase: 02-ux-consolidation
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/CatalogModal.tsx
  - src/components/WizardModal.tsx
  - src/components/CatalogBrowser.tsx
  - src/components/wizard/WizardShell.tsx
  - src/components/CommandPalette.tsx
  - src/routes/__root.tsx
autonomous: true
requirements: [UX-02, UX-03]

must_haves:
  truths:
    - "Clicking Browse Catalog button opens a large modal overlay with the catalog browser"
    - "Clicking Quick Setup Wizard button opens a modal overlay with the wizard"
    - "Catalog modal shows search + card grid without a FrontView preview pane"
    - "Wizard modal close replaces router navigate calls"
    - "CommandPalette Catalog/Wizard commands open modals instead of navigating"
    - "Closing either modal returns focus to the configurator behind it"
  artifacts:
    - path: "src/components/CatalogModal.tsx"
      provides: "Dialog wrapper around CatalogBrowser"
      exports: ["CatalogModal"]
    - path: "src/components/WizardModal.tsx"
      provides: "Dialog wrapper around WizardShell"
      exports: ["WizardModal"]
  key_links:
    - from: "src/components/CatalogModal.tsx"
      to: "src/store/useUIStore.ts"
      via: "useUIStore selectors for catalogModalOpen / closeCatalogModal"
      pattern: "useUIStore\\(s => s\\.catalogModal"
    - from: "src/components/WizardModal.tsx"
      to: "src/store/useUIStore.ts"
      via: "useUIStore selectors for wizardModalOpen / closeWizardModal"
      pattern: "useUIStore\\(s => s\\.wizardModal"
    - from: "src/components/CommandPalette.tsx"
      to: "src/store/useUIStore.ts"
      via: "useUIStore.getState() for opening modals from command palette"
      pattern: "useUIStore\\.getState\\(\\)"
---

<objective>
Convert Catalog and Wizard from full-page routes to modal overlays.

Purpose: Phase 1 UAT found that the catalog "goes full screen and blocks top bar" (test 10) and the wizard "buttons look bad, modal would be better" (test 11). This plan wraps both in Dialog overlays, adapts their internals for modal context, and updates CommandPalette to open modals instead of navigating.

Output: CatalogModal and WizardModal components mounted in root layout, CatalogBrowser adapted for modal mode, WizardShell adapted for modal lifecycle, CommandPalette updated.
</objective>

<execution_context>
@/Volumes/TB4 990 Evo 2Tb/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/TB4 990 Evo 2Tb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ux-consolidation/02-RESEARCH.md
@.planning/phases/02-ux-consolidation/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CatalogModal and WizardModal, adapt CatalogBrowser and WizardShell for modal mode</name>
  <files>
    src/components/CatalogModal.tsx
    src/components/WizardModal.tsx
    src/components/CatalogBrowser.tsx
    src/components/wizard/WizardShell.tsx
    src/routes/__root.tsx
  </files>
  <action>
    **Create `src/components/CatalogModal.tsx`:**
    - Import Dialog/DialogContent from `./ui/dialog`
    - Import useUIStore from `../store/useUIStore`
    - Import CatalogBrowser from `./CatalogBrowser`
    - Extract `catalogModalOpen` and `closeCatalogModal` at component top (hooks rule)
    - Render:
      ```tsx
      <Dialog open={open} onOpenChange={(v) => { if (!v) close(); }}>
        <DialogContent className="max-w-[90vw] w-[90vw] h-[85vh] p-0 overflow-hidden">
          <CatalogBrowser modal />
        </DialogContent>
      </Dialog>
      ```
    - The DialogContent may need additional classes to suppress the default close button padding or adjust for the full-bleed catalog layout. Check what `dialog.tsx` provides.

    **Create `src/components/WizardModal.tsx`:**
    - Same pattern as CatalogModal but for wizard
    - Import WizardShell from `./wizard/WizardShell`
    - Size: `max-w-[640px] w-full h-[80vh] p-0 overflow-hidden`
    - Pass `onClose={closeWizardModal}` to WizardShell so it can close the modal
    - On open, reset wizard state: either call a reset method on WizardShell or pass a key prop based on open state to force remount

    **Modify `src/components/CatalogBrowser.tsx`:**
    - Add `modal?: boolean` prop to the component
    - When `modal` is true: hide the FrontView preview panel (the right 40% column). Give 100% width to the search + card grid area.
    - When `modal` is true and user clicks "Add to Panel" on a catalog item: keep existing add behavior, optionally close the modal after adding (call `useUIStore.getState().closeCatalogModal()`)
    - Existing standalone behavior (if any non-modal usage remains) should be preserved via the `modal` prop default of `false`

    **Modify `src/components/wizard/WizardShell.tsx`:**
    - Add `onClose?: () => void` prop
    - Replace all `navigate({ to: '/' })` calls with `onClose?.()` (or a fallback if onClose not provided)
    - Replace `useBlocker()` usage: if the wizard has a blocker for unsaved changes, convert to a simple confirmation dialog inside the wizard (or remove if not critical for modal UX)
    - Remove `useNavigate` import from `@tanstack/react-router` if no longer used
    - The `handleCancel` and `handleEditInConfigurator` callbacks currently call navigate — these become `onClose?.()` calls
    - Reset wizard step to 0 on mount (add `useEffect(() => { setStep(0); }, [])` or clear sessionStorage key `rackpro-wizard-step` on mount)

    **Update `src/routes/__root.tsx`:**
    - Import CatalogModal and WizardModal
    - Add them to the render tree, after the Outlet and before Toaster:
      ```tsx
      <Outlet />
      <CatalogModal />
      <WizardModal />
      <Toaster position="bottom-center" />
      ```
    - These render as Dialog portals — they appear as overlays when open, and render nothing when closed (Radix Dialog behavior)
  </action>
  <verify>
    `npx tsc --noEmit` passes. `npx vite build` succeeds. Check that CatalogModal and WizardModal are imported in __root.tsx.
  </verify>
  <done>
    CatalogModal opens a large overlay with CatalogBrowser (no FrontView preview). WizardModal opens a medium overlay with WizardShell. Both are mounted in root layout. WizardShell uses onClose callback instead of router navigation. Catalog add-to-panel works from modal context.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CommandPalette to open modals instead of navigating, delete orphaned route files</name>
  <files>
    src/components/CommandPalette.tsx
    src/routes/catalog.tsx
    src/routes/wizard.tsx
    src/components/NavSidebar.tsx
  </files>
  <action>
    **Update `src/components/CommandPalette.tsx`:**
    - Find the "Navigation" command group (contains commands like "Configurator", "Catalog Browser", "Design Wizard")
    - Replace the `navigate({ to: '/catalog' })` call with `useUIStore.getState().openCatalogModal()` — use getState() because this is inside a callback, not a React render
    - Replace the `navigate({ to: '/wizard' })` call with `useUIStore.getState().openWizardModal()`
    - The "Configurator" command should close any open modal: `useUIStore.getState().closeCatalogModal(); useUIStore.getState().closeWizardModal();` — or simply do nothing since the configurator is always visible
    - Remove `useNavigate` import from `@tanstack/react-router` if no other commands use it
    - Rename the group from "Navigation" to something like "Tools" or "Open" since these are no longer page navigations
    - Import useUIStore at the top of the file

    **Delete orphaned files:**
    - Delete `src/routes/catalog.tsx` — this route file is no longer referenced by the router
    - Delete `src/routes/wizard.tsx` — this route file is no longer referenced by the router
    - Delete `src/components/NavSidebar.tsx` — this component is no longer imported anywhere after Plan 01 removed it from __root.tsx

    **Verify no remaining imports:**
    - Grep for `NavSidebar`, `catalog.tsx` (as import), `wizard.tsx` (as import) across the codebase
    - Grep for `navigate.*catalog` or `navigate.*wizard` to ensure all route-based navigation is replaced
    - Grep for `useNavigate` in files that should no longer need it
  </action>
  <verify>
    `npx tsc --noEmit` passes. `npx vite build` succeeds. `grep -r "NavSidebar" src/` returns no results. `grep -r "routes/catalog" src/` returns no results. `grep -r "routes/wizard" src/` returns no results. CommandPalette opens modals when Cmd+K -> Catalog/Wizard commands are selected.
  </verify>
  <done>
    CommandPalette Catalog/Wizard commands open modal overlays instead of navigating. All orphaned route files and NavSidebar.tsx are deleted. No stale imports remain in the codebase.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero TypeScript errors
2. `npx vite build` — production build succeeds
3. No imports of deleted files (NavSidebar, routes/catalog, routes/wizard) anywhere in src/
4. No `navigate.*catalog` or `navigate.*wizard` patterns remain
5. CatalogModal and WizardModal are rendered in __root.tsx
6. useUIStore is imported in CatalogModal, WizardModal, CommandPalette, and Sidebar
</verification>

<success_criteria>
- Clicking "Browse Catalog..." in sidebar opens a large modal with catalog search + cards (no FrontView preview)
- Clicking "Quick Setup Wizard" in sidebar opens a medium modal with the wizard steps
- Closing modals returns to the configurator (no page navigation)
- Cmd+K -> "Catalog Browser" opens the catalog modal
- Cmd+K -> "Design Wizard" opens the wizard modal
- Wizard starts at step 1 each time the modal opens (no stale sessionStorage step)
- No orphaned route or component files remain
</success_criteria>

<output>
After completion, create `.planning/phases/02-ux-consolidation/02-02-SUMMARY.md`
</output>
