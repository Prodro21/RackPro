# Plan 08-04: Remaining Components — Export, Specs, Catalog, Wizard, Modals

## Goal
Complete the theme migration for all remaining components. Remove any leftover legacy CSS variables. Final visual consistency pass. After this plan, the entire app presents as a cohesive light professional tool.

## Must-Haves

### Truths
- ExportTab: all 38 tiny text occurrences fixed, light card styling
- SpecsTab: all 46 dense spacing occurrences fixed, readable tables
- CatalogBrowser + CatalogCard + CatalogCardGrid: light cards, orange accents
- CatalogSearchSidebar: light background, readable filters
- All 7 wizard steps: proper button sizing, spacing, typography
- CustomDeviceModal: 20 tiny text occurrences fixed
- CommandPalette: light popover styling
- PreflightReport: readable text, proper severity colors
- UpdatePrompt: light dialog styling
- UI primitives (if needed): button, select, checkbox hover states correct for light
- No remaining legacy `--color-*` variable references anywhere in codebase
- Build passes, no visual regressions

### Artifacts
- `src/components/ExportTab.tsx`
- `src/components/SpecsTab.tsx`
- `src/components/CatalogBrowser.tsx`
- `src/components/CatalogCard.tsx`
- `src/components/CatalogCardGrid.tsx`
- `src/components/CatalogSearchSidebar.tsx`
- `src/components/wizard/*.tsx` (all 7 files)
- `src/components/CustomDeviceModal.tsx`
- `src/components/CommandPalette.tsx`
- `src/components/PreflightReport.tsx`
- `src/components/UpdatePrompt.tsx`
- `src/components/ui/*.tsx` (button, select, checkbox adjustments if needed)

## Key Links
- `08-CONTEXT.md` — Design decisions
- `08-RESEARCH.md` — Text size and spacing migration maps

## Execution

### Step 1: ExportTab.tsx
- Cards: `bg-card` with border, `p-4` padding
- All `text-[7-11px]` → `text-xs`/`text-sm`
- Format cards: white with orange accent on selected
- Preflight status: green/yellow/red badges on light background
- Download buttons: primary orange

### Step 2: SpecsTab.tsx
- Tables: clean borders, alternating subtle row backgrounds
- All dense spacing → `gap-2`/`gap-3` minimum
- Section headers: `text-base font-semibold`
- Values: `font-mono text-sm`
- Labels: sans-serif `text-sm text-muted-foreground`

### Step 3: Catalog components
- CatalogBrowser: `bg-background` with `gap-4` grid
- CatalogCard: white card, subtle shadow, orange hover border, readable specs
- CatalogCardGrid: proper grid gap
- CatalogSearchSidebar: light background, readable filter labels

### Step 4: Wizard steps
- WizardShell: light background, progress bar in orange
- StepStandard/StepUHeight: radio/card selectors with orange active state
- StepDevices/StepConnectors: list items with proper padding and typography
- StepReview: summary cards with light styling
- StepExport: export options with orange primary buttons
- StepNav: navigation buttons properly sized

### Step 5: Modals and overlays
- CustomDeviceModal: fix all 20 tiny text occurrences, light dialog
- CommandPalette: light popover, orange selected item highlight
- PreflightReport: readable text, severity badges with proper contrast
- UpdatePrompt: light dialog styling

### Step 6: UI primitives check
- Verify button, select, checkbox, slider, toggle all render correctly on light
- Check focus rings are orange
- Check hover states provide sufficient contrast

### Step 7: Legacy cleanup
- Grep for any remaining `color-accent-gold`, `color-text-*`, `color-bg-*`, `color-border-dim`, `color-danger`
- Remove from all files
- Verify no orphaned CSS vars in index.css

### Step 8: Final build + visual check
- `npm run build` — must pass clean
- All pages load without errors
