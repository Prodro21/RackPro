---
phase: 01.1-device-outline-mvp
plan: 04
subsystem: ui
tags: [svg, outlines, slug-normalization, cache, export]

# Dependency graph
requires:
  - phase: 01.1-03
    provides: Outline loading module, FrontView rendering, export config integration
provides:
  - Slug normalization mapping DEVICES constant keys to outline index slugs
  - All 10 priority devices resolve to correct outlines (3 via alias, 7 direct match)
  - Export JSON includes outlinePath for devices with top-face outlines (cache pre-populated by FrontView)
affects: [phase-2, phase-3, phase-6]

# Tech tracking
tech-stack:
  added: []
  patterns: [slug-alias-mapping, internal-slug-resolution]

key-files:
  created: []
  modified:
    - src/catalog/outlines.ts
    - src/components/FrontView.tsx

key-decisions:
  - "SLUG_ALIASES constant for hardcoded known mismatches rather than fuzzy matching -- explicit and auditable"
  - "Slug resolution inside all public API functions so callers never need to know about the mapping"
  - "Cache keys use resolved slugs so both short and full form keys hit the same cache entry"

patterns-established:
  - "Internal slug resolution: public APIs accept any key form, resolve internally before lookup"

requirements-completed: []

# Metrics
duration: 2min
completed: 2026-02-22
---

# Phase 1.1 Plan 04: Slug Normalization + Export Cache Fix Summary

**Slug alias mapping resolving DEVICES key mismatch (usw-lite-16 -> usw-lite-16-poe) with dual-face outline pre-loading for export outlinePath**

## Performance

- **Duration:** 2 min
- **Started:** 2026-02-22T06:05:35Z
- **Completed:** 2026-02-22T06:07:32Z
- **Tasks:** 2 (1 implementation + 1 verification)
- **Files modified:** 2

## Accomplishments
- Added `resolveOutlineSlug()` function with SLUG_ALIASES map resolving `usw-lite-16` -> `usw-lite-16-poe` and `usw-lite-8` -> `usw-lite-8-poe`
- Wired slug resolution into all four public API functions (hasOutline, getOutlineFaces, loadOutlinePath, getCachedOutlinePath) so all callers benefit automatically
- Updated FrontView to load both `front` and `top` face outlines, populating the cache that `configJson.ts` reads for export
- Verified all 10 priority devices resolve correctly: 3 DEVICES entries via alias/direct, 7 catalog entries via direct match

## Task Commits

Each task was committed atomically:

1. **Task 1: Add slug normalization to outlines.ts and wire into all consumers** - `cd2298c` (fix)
2. **Task 2: Verify the fix end-to-end with a build and static analysis** - verification only, no code changes

## Files Created/Modified
- `src/catalog/outlines.ts` - Added resolveOutlineSlug() with SLUG_ALIASES mapping; all public functions now resolve slugs internally
- `src/components/FrontView.tsx` - Updated outline loading useEffect to fetch both front and top faces for each device

## Decisions Made
- Used hardcoded SLUG_ALIASES constant rather than fuzzy/substring matching -- explicit, auditable, no false positives
- Placed slug resolution inside all public API functions so callers (FrontView, configJson) never need to import or call resolveOutlineSlug directly
- Cache keys use resolved slugs ensuring `loadOutlinePath('usw-lite-16', 'front')` and `getCachedOutlinePath('usw-lite-16-poe', 'front')` hit the same entry

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 1.1 gap closure complete -- all outline slug mismatches resolved
- All 10 priority devices will render with SVG outlines when placed
- Export JSON will include outlinePath for devices with top-face outlines after FrontView pre-loads them
- Ready to proceed to Phase 2 (Catalog Browser + Routing)

## Self-Check: PASSED

- [x] src/catalog/outlines.ts - FOUND
- [x] src/components/FrontView.tsx - FOUND
- [x] Commit cd2298c - FOUND

---
*Phase: 01.1-device-outline-mvp*
*Completed: 2026-02-22*
