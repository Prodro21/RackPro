---
phase: 01-catalog-schema
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - public/catalog/devices.json
  - public/catalog/connectors.json
autonomous: true
requirements:
  - CAT-01
  - CAT-02
  - CAT-05

must_haves:
  truths:
    - "50+ real network devices exist in devices.json with manufacturer-accurate dimensions"
    - "30+ real connectors exist in connectors.json with precise cutout specs matching CLAUDE.md reference"
    - "Every catalog entry has a dataSource confidence tier (manufacturer-datasheet, user-calipered, cross-referenced, or estimated)"
    - "Every entry has schemaVersion: 1 and a valid slug matching ^[a-z0-9-]+$"
    - "Device entries cover UniFi switches, gateways, access points, EdgeMAX legacy, and non-Ubiquiti gap fillers (compute, AV)"
    - "Connector entries use the cutout+module architecture: unique cutout geometry with compatibleModules array"
  artifacts:
    - path: "public/catalog/devices.json"
      provides: "50+ device entries with full dimensions, ports, confidence metadata"
      min_lines: 500
    - path: "public/catalog/connectors.json"
      provides: "30+ connector entries with cutout specs and compatible module lists"
      min_lines: 300
  key_links:
    - from: "public/catalog/devices.json"
      to: "src/catalog/schemas.ts"
      via: "JSON entries must validate against CatalogDeviceSchema"
      pattern: "schemaVersion.*1"
    - from: "public/catalog/connectors.json"
      to: "src/catalog/schemas.ts"
      via: "JSON entries must validate against CatalogConnectorSchema"
      pattern: "schemaVersion.*1"
---

<objective>
Author the JSON catalog data files containing 50+ verified devices and 30+ connectors with manufacturer-accurate dimensions.

Purpose: Populate the data foundation that every UI feature builds against. Without real data, the catalog schema is an empty shell.

Output: `public/catalog/devices.json`, `public/catalog/connectors.json`
</objective>

<execution_context>
@/Volumes/TB4 990 Evo 2Tb/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/TB4 990 Evo 2Tb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-catalog-schema/01-CONTEXT.md
@.planning/phases/01-catalog-schema/01-RESEARCH.md
@.planning/phases/01-catalog-schema/01-01-SUMMARY.md
@src/catalog/schemas.ts
@src/constants/devices.ts
@src/constants/connectors.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author public/catalog/devices.json with 50+ entries</name>
  <files>public/catalog/devices.json</files>
  <action>
Create `public/` and `public/catalog/` directories if they don't exist.

Author `public/catalog/devices.json` as a JSON array of objects conforming to `CatalogDeviceSchema`. Target: 50+ entries.

**Data sourcing strategy (from RESEARCH.md):**

| Category | Target Count | Notes |
|----------|-------------|-------|
| UniFi Switches (current gen) | 15-20 | USW-Lite-8-PoE, USW-Lite-16-PoE, USW-Pro-8-PoE, USW-Pro-24-PoE, USW-Pro-48-PoE, USW-Pro-Max-16-PoE, USW-Pro-Max-24-PoE, USW-Pro-Max-48-PoE, USW-Flex, USW-Flex-Mini, USW-Enterprise-8-PoE, USW-Enterprise-24-PoE, USW-Enterprise-48-PoE, USW-Mission-Critical, USW-Ultra |
| UniFi Gateways/Routers | 8-10 | UDM, UDM-Pro, UDM-SE, UDM-Pro-Max, UXG-Lite, UXG-Pro, UCG-Ultra, UniFi Express (UX), UniFi Express 7 (UX7), USG-3P (discontinued) |
| UniFi Access Points | 8-10 | U6-Lite, U6-LR, U6-Pro, U6-Enterprise, U6-Mesh, U7-Pro, U7-Pro-Max, U7-Pro-Wall |
| UniFi Other | 3-5 | UNAS-Pro, UNVR, UNVR-Pro, UCK-G2-Plus |
| EdgeMAX Legacy | 3-5 | ER-X, ER-4, EP-54V-150W, ES-24-Lite — all marked `discontinued: true` |
| Compute (non-Ubiquiti) | 3-4 | Raspberry Pi 5, Raspberry Pi 4B, Intel NUC 13 Pro, Beelink Mini PC |
| AV (non-Ubiquiti) | 2-3 | Blackmagic MicroConverter BiDirectional SDI/HDMI 12G, Blackmagic Web Presenter |

**IMPORTANT verified dimensions (from RESEARCH.md):**
- USW-Lite-16-PoE: 191.7 x 185 x 43.7mm, 1.2kg (datasheet verified)
- USW-Lite-8-PoE: 99.6 x 163.7 x 31.7mm, 0.295kg (datasheet — NOT the 200x119x30.3 in existing code)
- USW-Pro-8-PoE: 200 x 248 x 44mm, 2.1kg
- USW-Flex: 122.5 x 107.1 x 28mm, 0.23kg
- EdgeRouter X: 110 x 75 x 22mm, 0.175kg
- UniFi Express 7 (UX7): 117 x 117 x 42.5mm, 0.422kg
- Raspberry Pi 5: 86 x 56 x 16mm, 0.046kg

**For devices without verified dimensions:** Use `dataSource: "estimated"` or `dataSource: "cross-referenced"` and add a `notes` field explaining the source. Use best available data from manufacturer product pages.

**Per CONTEXT.md decisions:**
- Each SKU is a standalone entry (family grouping via `family` string field)
- Include `discontinued: true` for legacy products (USG-3P, ER-X, etc.)
- Include `ports` array with `pitch` where known. `positions` array is optional (leave out for most entries per RESEARCH.md Q2 recommendation — only populate for the 5-10 most popular devices if pitch data is available)
- Include WiFi access points (even though ceiling-mounted)
- No UISP or airMAX product lines

**Migrate existing 5 devices from `src/constants/devices.ts`** — ensure those slugs appear in the catalog with matching or corrected dimensions. Existing slugs: `usw-lite-16`, `ux7`, `usw-lite-8`, `usw-pro-max-16`, `usw-pro-24`.

**JSON format:** Array of objects. Each object has all required fields. Optional fields can be omitted (Zod .default() and .optional() handle them).

Validate the JSON file by running a quick Node.js script inline:
```bash
node -e "const d=require('./public/catalog/devices.json'); console.log('Entries:', d.length); const slugs=d.map(e=>e.slug); const dups=slugs.filter((s,i)=>slugs.indexOf(s)!==i); if(dups.length) console.error('DUPLICATE SLUGS:', dups); else console.log('No duplicate slugs'); console.log('Categories:', [...new Set(d.map(e=>e.category))]); console.log('Brands:', [...new Set(d.map(e=>e.brand))])"
```
  </action>
  <verify>
1. `node -e "const d=require('./public/catalog/devices.json'); console.log(d.length)"` prints 50 or more.
2. No duplicate slugs.
3. Every entry has `schemaVersion: 1`, `dataSource`, `width`, `depth`, `height`, `weight`.
4. Run `npx tsx -e "import {CatalogDeviceSchema} from './src/catalog/schemas'; const d=require('./public/catalog/devices.json'); let ok=0,bad=0; for(const e of d){const r=CatalogDeviceSchema.safeParse(e); if(r.success) ok++; else {bad++; console.error(e.slug,r.error.issues)}} console.log({ok,bad})"` — expect 0 bad entries.
  </verify>
  <done>50+ device entries exist in devices.json. All pass CatalogDeviceSchema validation. Entries cover UniFi switches, gateways, APs, EdgeMAX legacy, and non-Ubiquiti gap-fillers. All 5 existing device slugs are migrated. Every entry has a confidence tier in dataSource.</done>
</task>

<task type="auto">
  <name>Task 2: Author public/catalog/connectors.json with 30+ entries</name>
  <files>public/catalog/connectors.json</files>
  <action>
Author `public/catalog/connectors.json` as a JSON array of objects conforming to `CatalogConnectorSchema`. Target: 30+ unique cutout types with module variants.

**IMPORTANT: Use cutout+module architecture per CONTEXT.md decision.** Each entry defines a unique physical cutout shape. `compatibleModules` lists what can be installed in that cutout. Fabrication cares about the hole geometry, not the module.

**Connector catalog from RESEARCH.md + CLAUDE.md reference specs:**

| Connector | cutoutType | cutoutWidth | cutoutHeight | cutoutRadius | depthBehind | Notes |
|-----------|-----------|-------------|-------------|-------------|------------|-------|
| Neutrik D-Type | d-shape | 24 | 28 | 11.9 | 30 | Modules: XLR-M, XLR-F, EtherCon, opticalCON, BNC, HDMI, USB-A, USB-B, USB-C, SpeakON, PowerCON |
| Keystone Jack | rect | 16.5 | 19.2 | — | 28 | panelThicknessMax: 2. Modules: Cat5e, Cat6, Cat6a, HDMI, USB-A, USB-C, fiber LC, coax F |
| BNC Bulkhead (jam-nut) | round | 14 | 14 | 4.75 | 22 | 75-ohm SDI / 50-ohm RF |
| BNC Flange Mount | round | 14 | 14 | 4.75 | 22 | mountHoles: 4, mountHoleDiameter: 2.8, mountHoleSpacing: 12.7 |
| SMA Bulkhead | round | 9.5 | 9.5 | 3.175 | 15 | RF to 18 GHz |
| N-Type Bulkhead | round | 20 | 20 | 8 | 25 | RF to 11 GHz |
| TNC Bulkhead | round | 14 | 14 | 5.5 | 20 | RF |
| LC Duplex Fiber | rect | 26 | 18 | — | 32 | mountHoles: 2 |
| SC Simplex Fiber | rect | 16 | 20 | — | 35 | mountHoles: 2 |
| LGX Fiber Panel | rect | 130 | 40 | — | 45 | Holds multiple LC/SC adapters |
| DE-9 (DB9) | d-sub | 17.5 | 9.4 | — | 25 | mountHoles: 2 |
| DA-15 (DB15) | d-sub | 24 | 9.4 | — | 25 | mountHoles: 2 |
| DB-25 | d-sub | 40 | 9.4 | — | 25 | mountHoles: 2 |
| USB-A Panel Mount | rect | 13.1 | 5.6 | — | 25 | |
| USB-B Panel Mount | rect | 12 | 11 | — | 25 | |
| USB-C Panel Mount | rect | 9.5 | 3.5 | — | 20 | |
| HDMI Panel Mount | rect | 21 | 9.5 | — | 30 | |
| DisplayPort Panel Mount | rect | 19 | 8.5 | — | 28 | |
| IEC C14 Inlet | rect | 27.5 | 20 | — | 35 | Power inlet |
| IEC C13 Outlet | rect | 27.5 | 20 | — | 35 | Power outlet |
| PowerCON 20A (chassis) | round | 28 | 28 | 14 | 30 | Neutrik NAC3MP chassis |
| PowerCON TRUE1 (chassis) | round | 28 | 28 | 14 | 30 | Neutrik NAC3FPX chassis |
| XLR3 Chassis Mount | round | 22 | 22 | 9.5 | 25 | 3-pin audio |
| XLR5 Chassis Mount | round | 22 | 22 | 9.5 | 25 | 5-pin DMX |
| Speakon Chassis Mount | round | 28 | 28 | 12 | 30 | NL4/NL8 speaker |
| Banana Binding Post | round | 10 | 10 | 4 | 20 | Speaker/test |
| RCA Bulkhead (single) | round | 10 | 10 | 4.5 | 18 | Audio/video |
| RCA Bulkhead (dual) | rect | 22 | 10 | — | 18 | Stereo pair |
| 3.5mm TRS Panel Jack | round | 9 | 9 | 3 | 15 | 1/8" audio |
| 6.35mm TRS Panel Jack | round | 12 | 12 | 4.75 | 20 | 1/4" audio |
| DIN 5-pin (MIDI) | round | 18 | 18 | 7.5 | 20 | MIDI connector |
| Toslink Optical | rect | 10 | 10 | — | 18 | Digital audio |

That gives 32 unique cutout types. Populate `compatibleModules` for Neutrik D-type (10+ modules) and keystone (8+ modules). Other connectors may have 0-2 modules.

**Migrate existing connectors from `src/constants/connectors.ts`** — ensure those slugs appear. Existing slugs: `neutrik-d`, `bnc`, `rj45-ks`, `sma`, `lc-duplex`, `sc-simplex`, `de-9`, `usb-a`, `hdmi-a`, `iec-c14`.

Set `dataSource: "manufacturer-datasheet"` for connectors with precise specs from CLAUDE.md or datasheets. Use `cross-referenced` for entries where specs come from multiple community sources.

Validate the JSON:
```bash
node -e "const d=require('./public/catalog/connectors.json'); console.log('Entries:', d.length); const slugs=d.map(e=>e.slug); const dups=slugs.filter((s,i)=>slugs.indexOf(s)!==i); if(dups.length) console.error('DUPLICATE SLUGS:', dups); else console.log('No duplicate slugs')"
```
  </action>
  <verify>
1. `node -e "const d=require('./public/catalog/connectors.json'); console.log(d.length)"` prints 30 or more.
2. No duplicate slugs.
3. Run `npx tsx -e "import {CatalogConnectorSchema} from './src/catalog/schemas'; const d=require('./public/catalog/connectors.json'); let ok=0,bad=0; for(const e of d){const r=CatalogConnectorSchema.safeParse(e); if(r.success) ok++; else {bad++; console.error(e.slug,r.error.issues)}} console.log({ok,bad})"` — expect 0 bad entries.
4. Neutrik D-type entry has 10+ items in `compatibleModules`. Keystone entry has 8+ items.
  </verify>
  <done>30+ connector entries exist in connectors.json. All pass CatalogConnectorSchema validation. Cutout+module architecture is used: Neutrik D-type has 10+ modules, keystone has 8+ modules. All 10 existing connector slugs are migrated. Every entry has a confidence tier.</done>
</task>

</tasks>

<verification>
1. `public/catalog/devices.json` exists with 50+ entries, all passing Zod validation
2. `public/catalog/connectors.json` exists with 30+ entries, all passing Zod validation
3. No duplicate slugs in either file
4. All existing device and connector slugs from `src/constants/` are present in the catalog
5. Every entry has `schemaVersion: 1` and a `dataSource` confidence tier
6. Neutrik D-type has 10+ compatible modules, keystone has 8+ compatible modules
</verification>

<success_criteria>
- 50+ devices in devices.json with manufacturer-accurate dimensions where available
- 30+ connectors in connectors.json with precise cutout specs from CLAUDE.md reference
- Every entry has dataSource confidence tier
- All entries pass CatalogDeviceSchema / CatalogConnectorSchema validation
- Existing inline data slugs are all present in the new catalog
</success_criteria>

<output>
After completion, create `.planning/phases/01-catalog-schema/01-02-SUMMARY.md`
</output>
